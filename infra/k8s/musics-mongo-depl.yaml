apiVersion: apps/v1
kind: Deployment
metadata:
  name: musics-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: musics-mongo
  template:
    metadata:
      labels:
        app: musics-mongo
    spec:
      containers:
        - name: musics-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: musics-mongo-srv
spec:
  selector:
    app: musics-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
